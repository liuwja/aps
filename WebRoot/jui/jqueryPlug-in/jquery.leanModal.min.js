// leanModal v1.1 by Ray Stone - http://leanmodal.finelysliced.com.au
// Dual licensed under the MIT and GPL

(function ($) {
    $.fn.extend({ leanModal: function (options) {
        var defaults = { top: 100, closeButton: null }; 
        var overlay = $("<div id='lean_overlay'></div>"); 
        $("body").append(overlay); 
        options = $.extend(defaults, options); 
        alert(options.top+"   "+options.closeButton);
        return this.each(function () {
            var o = options; 
            $(this).click(function (e) {
            	var reg = new RegExp("^[1-9]*$");
                var obj = document.getElementById("printNum");
                if(!reg.test(obj.value)){
                	alert("请输入正确的数字!");
                	return false;
                }else{
                	$("#printNum2").text(obj.value);
                	$("#num").val(obj.value);
                	
                }	
                
                var modal_id = $(this).attr("href"); 
                $("#lean_overlay").click(function () { close_modal(modal_id) }); 
                $(o.closeButton).click(function () { close_modal(modal_id) }); 
                var modal_height = $(modal_id).outerHeight(); 
                var modal_width = $(modal_id).outerWidth();
                $("#lean_overlay").css({ "display": "block", opacity: 0 }); 
                $("#lean_overlay").fadeTo(200, o.overlay); 
                $(modal_id).css({ "display": "block", "position": "fixed", "opacity": 0, "z-index": 11000, "left": 50 + "%", "margin-left": -(modal_width / 2) + "px", "top": o.top + "px" }); 
                $(modal_id).fadeTo(200, 1); 
                e.preventDefault();
            })
        }); 
        function close_modal(modal_id) {
        	$("#lean_overlay").fadeOut(200); 
        	$(modal_id).css({ "display": "none" }) 
        }
    }
    })
})(jQuery);
